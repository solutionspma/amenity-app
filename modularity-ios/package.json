{
  "name": "modularity-spatial-os-ios",
  "version": "3.0.0",
  "description": "Native iOS ARKit/VRKit wrapper for Modularity Spatial OS - The Atomic Edition",
  "author": "Pitch Marketing Agency",
  "scripts": {
    "sync": "npm run copy-web && npx cap sync ios",
    "copy-web": "cp -r ../modularity-engine/* www/",
    "open": "npx cap open ios",
    "build": "npm run copy-web && npx cap sync ios && npx cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^5.0.0",
    "@capacitor/ios": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}
